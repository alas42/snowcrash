Find a file that has to do with the flag we are looking for shows results
It contains a file witch a ciphered passwd
after researches, find that it is a simple ceasar cipher -> get the right passwd (nottoohard) or something like that
